pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.17' }
  }
  environment {
    MARIADB_CREDS=credentials('mariadb-credentials')
  }
  stages {
    stage('Status') {
      steps {
        sh 'liquibase status --url="jdbc:postgresql://localhost:5432/postgres/my_app" --changeLogFile=my_app-wrapper.xml --username=postgres --password=fellaini'
      }
    }
    stage('Update') {
      steps {
        sh 'liquibase update --url="jdbc:postgresql://localhost:5432/postgres/my_app" --changeLogFile=my_app-wrapper.xml --username=postgres --password=fellaini'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}
